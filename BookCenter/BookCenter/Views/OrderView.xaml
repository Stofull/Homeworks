<UserControl x:Class="BookCenter.Views.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookCenter.Views"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Подтверждение заказа" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
            <TextBlock Text="Вы собираетесь купить следующую книгу:" Margin="0,0,0,5"/>
            <Button Content="Подтвердить заказ" Width="120"/>
            <Button Width="120" Command="{Binding GoBackCommand}">Отмена</Button>
        </StackPanel>
    </Grid>
</UserControl>
