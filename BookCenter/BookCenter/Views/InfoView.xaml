<UserControl x:Class="BookCenter.Views.InfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookCenter.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" >
            <Label Grid.Row="0" Content="{Binding SelectedBook.Title}"/>
            <Label Grid.Row="0" Content="{Binding SelectedBook.PageCount}"/>
            <Label Grid.Row="0" Content="{Binding SelectedBook.TitleSearch}"/>
            <Label Grid.Row="0" Content="{Binding SelectedBook.Copyright}"/>
            <Label Grid.Row="0" Content="{Binding SelectedBook.SeriesName}"/>



        </StackPanel>

        <Button
            Grid.Row="1"
            Command="{Binding OrderCommand}"
            Content="Order Ticket"/>
        <Button 
             Grid.Row="2"
             Command="{Binding GoBackCommand}">Go Back</Button> 
    </Grid>
</UserControl>
